# ══════════════════════════════════════════════════════════════
# Maven 项目级配置
# ══════════════════════════════════════════════════════════════
# 为什么需要这个文件：
#
# 本机 Maven 的全局 settings.xml（D:\develop\apache-maven-3.9.16\conf\settings.xml
# 第 56 行）把 localRepository 指向了 D:\develop\apache-maven-3.9.16\mvn_repo，
# 而这个位置：
#   1. 只有 2000+ 个 artifact，而 ~/.m2/repository 有 8000+，两份仓库不一致
#   2. 不含任何 Spring Boot 3.x 的 web starter（只有 4.0.8 / 4.1.0）
#   3. 没有 mybatis-spring-boot-starter、没有 flyway-core
#   4. 在某些环境下不可写
#
# 这会导致「IDE 和命令行结果不一致」「CI 上又是另一个环境」。
#
# 解决方案：让本项目的依赖仓库固定在项目目录下的 .m2repo/
#   - 不依赖任何机器的全局配置，谁 clone 下来行为都一样
#   - 已在 .gitignore 中忽略，不会提交
#   - 代价：首次构建需要联网下载（约 200-300 MB），之后都是增量
#
# ${maven.multiModuleProjectDirectory} 由 Maven 3.3.1+ 自动注入，
# 指向本文件所在的 .mvn 目录的上一级，即项目根目录。
# ══════════════════════════════════════════════════════════════

-Dmaven.repo.local=${maven.multiModuleProjectDirectory}/.m2repo
