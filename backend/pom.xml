<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ══════════════════════════════════════════════════════════
         AI 日记系统 —— 后端
         ══════════════════════════════════════════════════════════
         技术基线见 md文档/AI日记系统开发文档.md 第 2 章
         依赖冲突处理见该文档 §6.1
         ══════════════════════════════════════════════════════════ -->

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.sangshen</groupId>
    <artifactId>ai-diary-backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>ai-diary-backend</name>
    <description>AI 日记系统后端 —— Spring Boot 3 + MyBatis + MySQL</description>

    <properties>
        <!-- ⚠️ 必须与 JAVA_HOME 的 JDK 大版本一致，否则 maven-compiler-plugin 报
             "invalid target release"。本机请使用 JDK 17。 -->
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <mybatis-spring-boot.version>3.0.5</mybatis-spring-boot.version>
        <langchain4j.version>0.35.0</langchain4j.version>
        <maven-enforcer-plugin.version>3.5.0</maven-enforcer-plugin.version>

        <!-- ══════════════════════════════════════════════════════
             ⚠️ 以下三个属性是「覆盖 LangChain4j 的硬编码版本」
             ══════════════════════════════════════════════════════
             langchain4j-parent-0.35.0.pom 在它的 dependencyManagement
             里把下面这些「不属于它」的第三方库钉死了版本。而 Maven 的规则是
             dependencyManagement 压倒传递依赖的诉求，所以如果不覆盖，
             Spring Boot 3.3.5 的版本会被静默降级 —— 编译不报错，运行期才抛
             NoSuchMethodError。

             取值依据：spring-boot-dependencies-3.3.5.pom
               jackson-bom.version = 2.17.2
               okhttp.version      = 4.12.0
               slf4j.version       = 2.0.16

             注意：不要用 maven-enforcer-plugin 的 requireProperty 校验这些属性，
             因为 langchain4j-parent 把它们定义为默认值，requireProperty 只检查
             「属性是否存在」而非「在哪一层定义」，无法区分，会误报。
             真正有效的防线是 dependencyConvergence（见下方 build 段）。

             验证命令（Phase 0 验收标准之一）：
               mvn dependency:tree -Dincludes=com.squareup.okhttp3,com.fasterxml.jackson.core,org.jetbrains.kotlin
             ══════════════════════════════════════════════════════ -->
        <jackson.version>2.17.2</jackson.version>
        <okhttp.version>4.12.0</okhttp.version>
        <slf4j.version>2.0.16</slf4j.version>
    </properties>

    <dependencies>

        <!-- ── Web / REST ─────────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- ── 认证授权（Phase 1 启用，先占位保证依赖树稳定）───── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- ── 参数校验 ───────────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ── 健康检查 /actuator/health ──────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- ── 持久层 ─────────────────────────────────────────── -->
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>${mybatis-spring-boot.version}</version>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ── 数据库迁移 ─────────────────────────────────────── -->
        <!-- MySQL 8 需要 flyway-mysql 模块，只加 flyway-core 会启动失败 -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <!-- ── JSON（日记分析结果、Chroma metadata 都要用）────── -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- ── AI（Phase 3/4 使用）────────────────────────────── -->
        <!-- ⚠️ 三条硬规则，见开发文档 §6.1：
             1. 不引入 langchain4j-bom（会把它的 dependencyManagement 全量灌进来）
             2. 不引入 langchain4j-spring-boot-starter（带入额外自动配置，冲突面成倍扩大）
             3. 每个 artifact 显式写版本号
             所有调用必须包在 AiClient 接口后面。 -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j</artifactId>
            <version>${langchain4j.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-open-ai</artifactId>
            <version>${langchain4j.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-chroma</artifactId>
            <version>${langchain4j.version}</version>
        </dependency>

        <!-- ── 开发便利 ───────────────────────────────────────── -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ── 测试 ───────────────────────────────────────────── -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mysql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- ══════════════════════════════════════════════════
                 Maven Enforcer —— 本项目的核心防线
                 ══════════════════════════════════════════════════
                 把「运行期才炸的依赖冲突」变成「构建期红灯」。
                 详见开发文档 §6.1.3 第 ③ 条。

                 为什么比 requireProperty 有效：requireProperty 只能检查属性
                 是否存在，无法判断它定义在哪一层 POM、值有没有被覆盖；
                 dependencyConvergence 直接检查解析结果，覆盖不干净就会失败。
                 ══════════════════════════════════════════════════ -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven-enforcer-plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-dependency-convergence</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <!-- 同一 artifact 出现多个版本 → 构建失败
                                     并列出冲突路径与「建议固定为哪个版本」 -->
                                <dependencyConvergence/>
                            </rules>
                            <fail>true</fail>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

</project>
