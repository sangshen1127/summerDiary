# ─────────────────────────────────────────────────────────────
# 环境与密钥（最高优先级：绝不能提交）
# ─────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# 项目内 Maven 仓库（见 .mvn/maven.config，避免依赖被装到工作区外）
.m2repo/

# 项目内 npm 缓存（某些环境下全局缓存不可写时使用）
.npm-cache/

# ─────────────────────────────────────────────────────────────
# Java / Maven
# ─────────────────────────────────────────────────────────────
backend/target/
*.class
*.jar.original
*.log
hs_err_pid*.log
replay_pid*.log

# ─────────────────────────────────────────────────────────────
# Node / 前端
# ─────────────────────────────────────────────────────────────
node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/test-results/
frontend/playwright-report/
frontend/.eslintcache
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ─────────────────────────────────────────────────────────────
# IDE
# ─────────────────────────────────────────────────────────────
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.swp
*~

# ─────────────────────────────────────────────────────────────
# 操作系统
# ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ─────────────────────────────────────────────────────────────
# Docker 数据卷（如果改用本地目录挂载）
# ─────────────────────────────────────────────────────────────
docker/volumes/

# ─────────────────────────────────────────────────────────────
# 临时探索目录（AI Agent 排查环境时用，不属于项目）
# ─────────────────────────────────────────────────────────────
_probe/

# ─────────────────────────────────────────────────────────────
# 接口验收脚本（保留源码便于复现，但编译产物和临时数据不入库）
# ─────────────────────────────────────────────────────────────
_verify/*.class
# 验收脚本的输出重定向文件（跑一次生成一堆，看完就该删）
_verify/_out_*.txt

# ─────────────────────────────────────────────────────────────
# 排障时的一次性输出（后台起服务的日志、编译日志等）
# 只在排查当时有意义，不要入库；正常日志请用自己的重定向路径
# ─────────────────────────────────────────────────────────────
_out_*.txt

# ─────────────────────────────────────────────────────────────
# DSH 技能目录
# 想和同学共享技能就删掉下面这行；只想本地用就保留。
# ─────────────────────────────────────────────────────────────
# .dsh/

# ─────────────────────────────────────────────────────────────
# 浏览器验收产物
# _verify/shots/edge-profile 是 headless Edge 的「用户数据目录」
# （905 个文件 / 32MB 的缓存、Cookie、扩展缓存），每次跑
# browser-check.mjs 都会重建，属于纯临时数据，绝不能入库。
# ⚠️ 只忽略这个子目录：_verify/shots/*.png 是验收截图，
#    它们是「前端真的渲染正确」的证据，要保留。
# ─────────────────────────────────────────────────────────────
_verify/shots/edge-profile/

# ─────────────────────────────────────────────────────────────
# Claude Code 的本地设置（含本机权限允许列表，因人而异）
# ─────────────────────────────────────────────────────────────
.claude/settings.local.json
